import React from 'react';

const Gallery = () => {
    // Placeholder for gallery items depending on what user wants to show (projects, certs, etc.)
    return (
        <section id="gallery" className="gallery-section">
            <h2>Gallery</h2>
            <div className="gallery-grid">
            </div>
        </section>
    );
};

export default Gallery;
